def getPrinterStatuses():
    """
    pre) we need to get print status, file name, and printer number
    1) just get the status of everything on all the printers
    2) if a print has failed we update jira and try again
    3) if a print has finished we update jira
    """
def uploadFileToPrinter:
    """
    1) if we have a file in downloads
    2) get list of open printers
    3) if printer is open move file to printer and print
    4) delete original file from server
    5) update jira that print has started
    """
